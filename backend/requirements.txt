# ── Core Framework ──────────────────────────────────────────────
# Use loose version pins to allow pip to find Python 3.14-compatible wheels
Flask>=3.0.3
Werkzeug>=3.0.3

# ── Database ─────────────────────────────────────────────────────
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.7
SQLAlchemy>=2.0.36     # 2.0.36+ required for Python 3.13/3.14 compatibility

# ── Security ─────────────────────────────────────────────────────
Flask-WTF>=1.2.1
Flask-Limiter>=3.5.1
flask-cors>=4.0.1
bleach>=6.1.0

# ── Validation ───────────────────────────────────────────────────
# pydantic v1: pure Python (no Rust), Python 3.14 compatible
pydantic==1.10.21

# ── File Handling ────────────────────────────────────────────────
# filetype: pure-Python magic-byte MIME detection
filetype>=1.2.0

# ── AI / RAG ─────────────────────────────────────────────────────
# rank-bm25: BM25 Okapi sparse retrieval — pure Python, Python 3.14 ✓
rank-bm25>=0.2.2
# numpy: cosine similarity re-ranking — wheel available for Python 3.14 ✓
numpy>=1.26.0

# ── Environment ──────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── WSGI Servers ─────────────────────────────────────────────────
waitress>=3.0.0        # Windows dev
gunicorn>=21.2.0       # Linux prod (install only on Linux)

# ── Testing ──────────────────────────────────────────────────────
pytest>=8.2.0
pytest-flask>=1.3.0
